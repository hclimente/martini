GIN_PATH=`${R_HOME}/bin/Rscript -e "cat(Rgin:::.pkgLd())"`
GIN_LIBS="$(GIN_PATH)/libgin.a" "$(GIN_PATH)/libmaxflow.a" "$(GIN_PATH)/libcephes.a" -lz -pthread
GIN_CPPFLAGS=-DAS_GINLIB -DAS_RGINLIB
	
PKG_LIBS=$(GIN_LIBS) $(SHLIB_OPENMP_CXXFLAGS)
PKG_CPPFLAGS=$(GIN_CPPFLAGS) $(SHLIB_OPENMP_CXXFLAGS)